export const meta = {
  slug: "drop-002",
  title: "Drop #002 — Signal and Structure",
  summary:
    "A second release to validate the drop pipeline: featured selection, ordering, and shareable routing.",
  date: "2026-01-12",
  tags: ["drops", "structure"],
  type: "drop",
  series: "Drops",
  featured: true,
};

# Drop #002 — Signal and Structure

This is the second Drop.

## What it is

A clean test of the system: metadata, sorting, featured selection, and prerender enumeration.

## What to verify

- Home “Featured Drop” now points to **Drop #002**
- `/content` shows newest items first
- `/content/drop-002` renders and has correct title/description

## What’s next

Next/previous navigation and SEO polish across the content library.
