import type { Config } from 'vike/types'

export default {
  title: 'Content — The Neighborhood On The Block',
  description: 'Drops, loops, and experiments — each with a dedicated, shareable page.'
} satisfies Config
